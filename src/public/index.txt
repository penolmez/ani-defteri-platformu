<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PortalHane Kompakt Panel</title>
    <style>
        :root {
            --primary: #2c3e50;
            --accent: #d35400;
            --bg: #f3f4f6;
            --white: #ffffff;
            --border: #e5e7eb;
        }

        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background-color: var(--bg); 
            margin: 0; 
            padding: 20px;
            color: var(--primary);
        }

        .container { 
            max-width: 1400px; 
            margin: 0 auto; 
            background: var(--white);
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        h1 { margin-top: 0; font-size: 1.5rem; text-align: center; margin-bottom: 20px;}
        h2 { font-size: 1.1rem; border-bottom: 2px solid var(--border); padding-bottom: 5px; margin-top: 25px; margin-bottom: 15px; color: var(--accent); text-transform: uppercase; letter-spacing: 1px;}

        /* --- ÜST KISIM: MÜŞTERİ ADI --- */
        .header-section {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }
        .header-input {
            width: 50%;
            padding: 12px;
            font-size: 1.1rem;
            border: 2px solid var(--accent);
            border-radius: 8px;
            text-align: center;
            background: #fff5f0;
        }

        /* --- GRID SİSTEMLERİ --- */
        
        /* Fotoğraf Grid'i: Kare kutular, yan yana */
        .photo-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
            gap: 10px;
        }

        /* Metin Grid'i: Daha geniş kutular */
        .text-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); /* Sığabildiği kadar yan yana */
            gap: 12px;
        }

        /* --- BİLEŞENLER --- */

        /* Metin Kutuları */
        input[type="text"], textarea { 
            width: 100%; 
            padding: 10px; 
            border: 1px solid var(--border); 
            border-radius: 6px; 
            font-size: 0.9rem; 
            box-sizing: border-box; 
            background: #f9fafb;
            transition: all 0.2s;
        }
        input[type="text"]:focus, textarea:focus {
            border-color: var(--accent);
            background: var(--white);
            outline: none;
            box-shadow: 0 0 0 3px rgba(211, 84, 0, 0.1);
        }
        textarea { resize: vertical; height: 42px; min-height: 42px; font-family: inherit;}

        /* Drop Zone (Fotoğraf Kutusu) */
        .drop-zone {
            aspect-ratio: 1; /* Tam kare olması için */
            border: 2px dashed #cbd5e1;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            font-size: 0.75rem;
            color: #64748b;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: all 0.2s;
            background: #f8fafc;
        }
        .drop-zone:hover { border-color: var(--accent); background: #fff; }
        .drop-zone--over { background-color: #ffe4d6; border-color: var(--accent); border-style: solid;}
        
        .drop-zone__prompt { padding: 5px; pointer-events: none; font-weight: 600;}
        .drop-zone__input { display: none; }
        
        /* Yüklenen Resim Önizleme */
        .drop-zone__thumb {
            width: 100%; height: 100%;
            background-size: cover; background-position: center;
            position: absolute;
        }
        /* Resim üzerindeki etiket */
        .drop-zone__thumb::after {
            content: attr(data-label);
            position: absolute; bottom: 0; left: 0; width: 100%;
            background: rgba(0,0,0,0.6); color: #fff;
            font-size: 0.7rem; padding: 2px 0;
        }

        /* Buton */
        .submit-btn {
            width: 100%;
            padding: 15px;
            margin-top: 30px;
            background-color: var(--primary);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s;
        }
        .submit-btn:hover { background-color: #1a252f; }

        /* Metin grupları için küçük başlıklar (Opsiyonel, yer tutucu yeterliyse kaldırılabilir) */
        .group-label {
            grid-column: 1 / -1;
            font-size: 0.8rem;
            color: #999;
            margin-top: 10px;
            margin-bottom: -5px;
            text-transform: uppercase;
            font-weight: bold;
        }
    </style>
</head>
<body>

<div class="container">
    <form id="autoForm" enctype="multipart/form-data">
        
        <div class="header-section">
            <input type="text" name="musteri_adi" class="header-input" required placeholder="MÜŞTERİ ADI / KLASÖR ADI GİRİNİZ...">
        </div>

        <h2>FOTOĞRAFLAR</h2>
        <div class="photo-grid">
            <div class="drop-zone"><span class="drop-zone__prompt">Netflix Kapağı</span><input type="file" name="08_Sayfa" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Spotify Kapağı</span><input type="file" name="09_Sayfa" class="drop-zone__input" accept="image/*"></div>
            
            <div class="drop-zone"><span class="drop-zone__prompt">Ön Kapak</span><input type="file" name="01_Sayfa" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Arka Kapak</span><input type="file" name="16_Sayfa" class="drop-zone__input" accept="image/*"></div>
            
            <div class="drop-zone"><span class="drop-zone__prompt">Sayfa 14</span><input type="file" name="14_Sayfa" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Sayfa 15</span><input type="file" name="15_Sayfa" class="drop-zone__input" accept="image/*"></div>
            
            <div class="drop-zone"><span class="drop-zone__prompt">Yırtık 1</span><input type="file" name="03_Sayfa_1" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Yırtık 2</span><input type="file" name="03_Sayfa_2" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Şerit 1</span><input type="file" name="06_Sayfa" class="drop-zone__input" accept="image/*"></div>
            <div class="drop-zone"><span class="drop-zone__prompt">Şerit 2</span><input type="file" name="07_Sayfa" class="drop-zone__input" accept="image/*"></div>
        </div>

        <h2>METİNLER</h2>
        <div class="text-grid">
            
            <div class="group-label">Netflix Bilgileri</div>
            <input type="text" name="Text_Film_Baslik" placeholder="Film Başlığı (Vars: SONSUZ AŞK...)">
            <textarea name="Text_Film_Aciklama" placeholder="Film Açıklaması..."></textarea>
            <input type="text" name="Text_Film_Oyuncular" placeholder="Oyuncular (İsim, İsim)">
            <input type="text" name="Text_Film_Turler" placeholder="Tür (Aşk, Romantizm)">
            <input type="text" name="Text_Film_Sure" placeholder="Süre (Sonsuza Kadar)">

            <div class="group-label">Spotify Bilgileri</div>
            <input type="text" name="Text_Sarki_Ad" placeholder="Şarkı Adı (Vars: BÖYLE SEVER)">
            <input type="text" name="Text_Sarki_Sanatci" placeholder="Sanatçı (Vars: Kahraman Deniz)">

            <div class="group-label">Sayfa 14 & 15</div>
            <input type="text" name="Text_14_Sayfa_Ust" placeholder="Sayfa 14 Üst Başlık (Senİ Çoooook Sevİyorum)">
            <textarea name="Text_14_Sayfa_Alt" placeholder="Sayfa 14 Alt Metin (Seninle geçirdiğim her an çok özel, iyi ki hayatımdasın. Seni bir ömür seveceğime söz veriyorum.)"></textarea>
            <input type="text" name="Text_15_Sayfa_Ust" placeholder="Sayfa 15 Üst Başlık (İyİ Kİ “BİZ”)">
            <textarea name="Text_15_Sayfa_Alt" placeholder="Sayfa 15 Alt Metin (Sen ve Ben, bizden başka hiç kimsenin anlayamacağı bir hikayeyiz.)"></textarea>

            <div class="group-label">Diğer Sayfalar</div>
            <input type="text" name="Text_03_Sayfa" placeholder="Yırtık Kağıt Metni (Bazı Ruhlar Evvelden Aşinadır Birbirine )">
            <input type="text" name="Text_06_Sayfa" placeholder="Şerit 1 Metni (Seninle)">
            <input type="text" name="Text_07_Sayfa" placeholder="Şerit 2 Metni (Bir Ömür)">

            <div class="group-label">Kapak Yazıları</div>
            <textarea name="Text_Ana_Baslik" placeholder="Ön Kapak (Our Memories)"></textarea>
            <textarea name="Text_Arka_Baslik" placeholder="Arka Kapak (I Love You)"></textarea>
        </div>

        <button type="submit" class="submit-btn">GÖNDER VE OLUŞTUR</button>
    </form>
</div>

<script>
    // --- DRAG AND DROP / GÖRSEL ÖNİZLEME JS ---
    document.querySelectorAll(".drop-zone__input").forEach((inputElement) => {
        const dropZoneElement = inputElement.closest(".drop-zone");

        dropZoneElement.addEventListener("click", () => inputElement.click());

        inputElement.addEventListener("change", () => {
            if (inputElement.files.length) updateThumbnail(dropZoneElement, inputElement.files[0]);
        });

        dropZoneElement.addEventListener("dragover", (e) => {
            e.preventDefault();
            dropZoneElement.classList.add("drop-zone--over");
        });

        ["dragleave", "dragend"].forEach((type) => {
            dropZoneElement.addEventListener(type, () => dropZoneElement.classList.remove("drop-zone--over"));
        });

        dropZoneElement.addEventListener("drop", (e) => {
            e.preventDefault();
            dropZoneElement.classList.remove("drop-zone--over");
            if (e.dataTransfer.files.length) {
                inputElement.files = e.dataTransfer.files;
                updateThumbnail(dropZoneElement, e.dataTransfer.files[0]);
            }
        });
    });

    function updateThumbnail(dropZoneElement, file) {
        let thumbnail = dropZoneElement.querySelector(".drop-zone__thumb");
        if (dropZoneElement.querySelector(".drop-zone__prompt")) dropZoneElement.querySelector(".drop-zone__prompt").style.display = 'none';
        
        if (!thumbnail) {
            thumbnail = document.createElement("div");
            thumbnail.classList.add("drop-zone__thumb");
            dropZoneElement.appendChild(thumbnail);
        }
        thumbnail.dataset.label = file.name; // Yüklenen dosyanın adını göster
        
        const reader = new FileReader();
        reader.readAsDataURL(file);
        reader.onload = () => thumbnail.style.backgroundImage = `url('${reader.result}')`;
    }

    // --- FORM GÖNDERME ---
    document.getElementById('autoForm').addEventListener('submit', async function(e) {
        e.preventDefault();
        const formData = new FormData(this);
        const btn = document.querySelector('.submit-btn');
        const originalText = btn.innerText;
        
        btn.innerText = "KAYDEDİLİYOR...";
        btn.disabled = true;

        try {
            const res = await fetch('/api/olustur', { method: 'POST', body: formData });
            const result = await res.json();
            if (result.success) {
                alert("Başarılı!");
                window.location.reload();
            } else {
                alert("Hata: " + result.error);
                btn.innerText = originalText;
                btn.disabled = false;
            }
        } catch (error) {
            alert("Sunucu Hatası!");
            btn.innerText = originalText;
            btn.disabled = false;
        }
    });
</script>

</body>
</html>